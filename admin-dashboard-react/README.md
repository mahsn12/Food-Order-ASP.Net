# Food Delivery Admin Dashboard (React)

Simple React dashboard to manage all admin CRUD endpoints.

## Run

```bash
npm install
npm run dev
```

The app expects the API at `http://localhost:5147` by default.

To override:

```bash
VITE_API_BASE_URL=http://localhost:5000 npm run dev
```
